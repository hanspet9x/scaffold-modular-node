export interface UserEntity {
  user: string;
  password: string;
  email: string;
  firstName: string;
  lastName: string;
  address: string;
  phoneNumber: string;
  officeNumber: string;

};
